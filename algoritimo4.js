const arr = [1, 2, 3, 4, 5]; // Criação de um array de números.

const output = arr.filter((x) => x > 4); // Aplica o filtro no array 'arr', selecionando apenas os números maiores que 4.

console.log(output); // Exibe no console o resultado do filtro, que será um array com os valores [5].
