const numbers = [1, 2, 3, 4, 5]; // Cria um array de números

const output = numbers.map((x) => { // aqui é ultilizado o método map na variavel já criada numbers

return x * 2;// Retorna o valor de 'x' multiplicado por 2.
});

console.log(output); //Exibe no console o resultado de output que contém o resultado de map
